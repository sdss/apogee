#/bin/csh
foreach dir ( lco25m/lco25m_* apo25m/apo25m_* apo1m/apo1m_*)
  set field = `echo $dir | awk -F/ '{print $2}'`
  set tel = `echo $dir | awk -F/ '{print $1}'`
  echo $field
  if ( -e log/"$field".yml.done ) then
    echo DONE
  else
    ls -lt $dir/ferre | head -2 | tail -1
  endif

  #set n = `ls log/"$field"_"$tel"*.kp??? | wc -l`  
  #echo $dir $n
  #if ( $n>0 ) ls -ld $dir/ferre
end

